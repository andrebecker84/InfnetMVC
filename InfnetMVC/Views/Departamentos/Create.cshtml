@model InfnetMVC.Models.Departamento

@{
    ViewData["Title"] = "Criar Departamento";
}

<style>
    .cover-container {
        max-width: 80%;
        border-radius: 3rem;
    }

    .card-body-2 {
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .wide-input-reg {
        width: 400px;
        height: 40px;
    }

    .botoes {
        justify-content: center;
        display: flex;
        gap: 1rem;
    }

    .label {
        margin-top: 1rem;
        margin-left: 1rem;
        margin-bottom: 0.5rem;
        text-align: left;
        font-weight: bold;
    }
</style>        

<div class="d-flex card shadow p-5 cover-container mx-auto">
    <h2 class="text-center"><i class="fa-solid fa-building"></i> @ViewData["Title"]</h2>
    <hr />
    <div class="d-flex card-body-2 flex-column align-items-center">
        <form asp-action="Create" class="w-100">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>
            <div class="formulario">
                <div class="form-group my-2">
                    <div class="label">
                        <label asp-for="Nome" class="control-label"></label>
                    </div>
                    <input asp-for="Nome" class="shadow form-control rounded-pill wide-input-reg" autocomplete="nome" aria-required="true" placeholder=" Digite o nome..." />
                    <span asp-validation-for="Nome" class="text-danger"></span>
                </div>
                <div class="form-group my-2">
                    <div class="label">
                        <label asp-for="Local" class="control-label"></label>
                    </div>
                    <input asp-for="Local" class="shadow form-control rounded-pill wide-input-reg" autocomplete="local" aria-required="true" placeholder=" Digite o local..." />
                    <span asp-validation-for="Local" class="text-danger"></span>
                </div>
                <div class="botoes mt-4">
                    <button type="submit" value="Create" class="shadow btn btn-primary rounded-pill p-2"><i class="fa-solid fa-plus"></i> Criar</button>
                    <button asp-action="Index" class="shadow btn btn-secondary rounded-pill p-2"><i class="fa-solid fa-arrow-left"></i> Voltar</button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}